kind = "mobile"
previewPath = "/mobile/"
title = "NEXUS Mobile"
version = "1.0.0"
id = "artifacts/nexus-mobile"
router = "expo-domain"

[[services]]
name = "expo"
ensurePreviewReachable = "/status"
paths = [ "/mobile/" ]
localPort = 19174

[services.development]
run = "pnpm --filter @workspace/nexus-mobile run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/nexus-mobile", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/nexus-mobile", "run", "serve" ]

[services.env]
PORT = "19174"
BASE_PATH = "/mobile/"
